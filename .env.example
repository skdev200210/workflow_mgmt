# PostgreSQL connection (async driver: asyncpg)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/agents

# App
APP_NAME=Agent Service
DEBUG=true

# Required in the X-API-Key header for all /agents routes.
API_KEY=changeme-dev-key
